sudo service nginx stop
apps=$(grep server_name -m 1 /etc/nginx/conf.d/* | awk -F' ' '{print $3}' | awk -F';' '{print $1}')
for i in `echo $apps`
do
	sudo /usr/bin/letsencrypt certonly --standalone -d $i
done
sudo service nginx start